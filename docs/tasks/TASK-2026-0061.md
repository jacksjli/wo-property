【任务分配单】TASK-2026-0061
━━━━━━━━━━━━━━━━━━━━━━━
任务名称：PersonService + MasterDataService 实现方案 & MySQL 统一迁移方案

负责人：软件架构师
协助：后端工程师、数据库工程师
优先级：P0
截止时间：2026-05-06 14:00（供次日评审会使用）

背景：
  甲方明确三件事必须实现：
  1. PersonService (5018) 和 MasterDataService (5019) 必须实现
  2. 14 个微服务之间必须有真正的 HTTP 通信机制
  3. 所有服务统一使用 MySQL，放弃 SQLite

  当前状态：
  - PersonService 和 MasterDataService 目录存在，但无 Program.cs（服务不存在）
  - 各服务完全独立，无任何服务间调用
  - 除 FinanceService 外，其他服务均使用 SQLite

交付标准：
  1. PersonService API 规范文档（/docs/architecture/person-service-api.md）
     - 包含数据模型、API 端点、认证方式
  2. MasterDataService API 规范文档（/docs/architecture/masterdata-service-api.md）
     - 包含数据模型、API 端点、枚举值管理
  3. 服务间通信协议（/docs/architecture/service-communication.md）
     - HTTP 调用约定、错误处理、降级策略
  4. MySQL 统一迁移方案（/docs/architecture/mysql-migration.md）
     - 数据库规划、分批迁移步骤、数据保护策略
  5. 实施时间表（/docs/architecture/implementation-plan.md）
     - 分解为可执行的子任务，包含人力估算

交付物模板：
  每个文档需包含：
  - 目标：解决什么问题
  - 方案：具体设计
  - 风险：可能的障碍及应对
  - 决策点：需要甲方或软件负责人确认的问题

前置依赖：
  - 需要甲方在 2026-05-06 14:00 前确认数据库策略（统一大库 vs 分库）
  - 需要甲方确认服务间调用错误处理策略

风险项：
  - MySQL 迁移期间数据保护策略尚未确认
    → 解决方案：先输出方案，等甲方决策后再执行
  - 服务间调用可能引入循环依赖
    → 解决方案：禁止服务间循环调用，PersonService/MasterDataService 不调用其他业务服务
  - SQLite 开发环境和 MySQL 生产环境差异
    → 解决方案：开发环境也使用 MySQL（Docker），统一环境

任务拆分：
  子任务 A（架构师）：
    - 输出 PersonService API 规范（Deadline：明天 10:00）
    - 输出 MasterDataService API 规范（Deadline：明天 10:00）
    - 输出服务间通信协议（Deadline：明天 12:00）

  子任务 B（数据库工程师）：
    - MySQL 数据库规划方案（统一大库 vs 分库）建议（Deadline：明天 10:00）
    - 数据迁移脚本设计（Deadline：明天 12:00）

  子任务 C（后端工程师）：
    - PersonService 骨架代码搭建（等服务名、端口确认后）
    - MasterDataService 骨架代码搭建

会议安排：
  2026-05-06 14:00 - 架构方案评审会
  - 参与者：软件架构师、后端工程师、数据库工程师、软件负责人
  - 议程：评审 API 规范、MySQL 方案、时间表
  - 输出：确认可执行的实施计划

━━━━━━━━━━━━━━━━━━━━━━━
```

**创建人**：软件负责人
**创建时间**：2026-05-05 07:43
**最后更新**：2026-05-05 07:43
